<!doctype html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        
        <title><%= pagetitle %></title>
        
        <link rel="stylesheet" href="/design/bootstrap/bootstrap.min.css">
        <link rel="stylesheet" href="/design/css/custom.css">
        <% if(typeof extraHead !== 'undefined') { %>
            <%- extraHead %> <%# Not sure, if it should be - or =, becuase here we paste script. Might be - is the right %> 
        <% } %> 
    </head>
    <body id="<%= id %>">